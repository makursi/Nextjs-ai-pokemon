# Canonical origin of the deployed site, used by metadata, sitemap and robots.
# Leave it unset for local development: the app falls back to http://localhost:3000.
# Set it in production, e.g.
# SITE_URL=https://example.com
